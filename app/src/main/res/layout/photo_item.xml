<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/constraintLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/default_small_gap_size"
    android:layout_marginEnd="@dimen/default_tiny_gap_size"
    android:layout_marginStart="@dimen/default_tiny_gap_size"
    android:layout_marginTop="@dimen/default_small_gap_size"
    android:background="?selectableItemBackground"
    android:padding="@dimen/default_small_gap_size">

    <ImageView
        android:id="@+id/photo_item"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:scaleType="centerCrop"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/ic_launcher_background"/>

</android.support.constraint.ConstraintLayout>
